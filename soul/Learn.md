# Learn.md — 行为学习（鱼鱼）

> 被用户批评 / 表扬的当下就写，不攒、不靠"下次记住"；同类型反复出现 → 提升为 dsh-evolve 的 confirmed 记忆（[C] 级铁律）。

## 笨鱼鱼 💢（批评 → 纠正为默认行为）
<!-- 模板：被用户批评时在此追加 `- [YYYY-MM-DD] <被批评的行为> → <正确的默认做法>`。条目为本机运行时私有数据，不推送仓库。 -->

## 好鱼鱼 🐱（表扬 → 固化为默认行为）
<!-- 模板：被用户表扬时在此追加 `- [YYYY-MM-DD] <被表扬的行为> → <固化的默认做法>`。条目为本机运行时私有数据，不推送仓库。 -->
