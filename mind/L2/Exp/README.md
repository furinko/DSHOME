# Exp — 工具手册库（mind\L2\Exp\）

> 版本：1.2 | 2026-09-24 | 收下**首个出厂 Exp**：`mind-api-calls`（由私有区**提升**、去敏改写为固定四章；私有引用改「按标题引用 + 标注私有区」）| 1.1 | 2026-09-18 | **§ 引用对齐实测**：触发机制在 `Power.md` **§二**（原写 §三＝沉淀路径）、格式规范在 **§四**（原写 §六**不存在**——Power 只到 §四）——原文两处都把人指向错/不存在的节
> 定位：L2 能力层——外部工具/平台/引擎的使用手册（部署/接入/核心操作/踩坑）。
> 触发：工具名或场景关键词（详见 mind\L1\Power.md §二）。
> 格式：frontmatter + 固定四章（工具定位/部署接入/核心操作/关联索引），见 mind\L1\Power.md §四。

## 当前 Exp

| Exp | 版本 | 描述 |
|---|---|---|
| `mind-api-calls` | 1.0.0 | 用脚本直打 `/api/mind/*` 的三条硬口径：CSRF 头 `Sec-Fetch-Site: same-origin` · body 必须 **UTF-8 字节**（字符串 body 会把中文**静默**变 `?`，接口仍返 200）· 写完**回读校验**；附已实测路由形状与「`cron/run` 会真拉一个 agent」的边界 |
