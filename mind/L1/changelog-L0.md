# changelog-L0.md — L0 宪法版本沿革台账（出厂区）

> 定位：`mind\L0\SOUL.md` / `AGENTS.md` 的**历史版本沿革全文**归档。**不进 R0 注入**——L0 正文尾部只留一行本版摘要 + 指向本文件的指针。
> 动机（2026-09-23，主人授权「你决定」）：L0 是**每会话注入**的宪法，而版本沿革占 SOUL 正文 26.7%、AGENTS 正文 22.1% 的字符——违反 L0 自订原则"每行都要值得每个会话看到"。移出 = 每会话省 ~1,600 字符注入（且每轮随历史重发）。
> 维护约定：改 SOUL/AGENTS 时，**正文尾部只写一行本版摘要**，被替换下来的旧沿革整行**追加到本文件**（AGENTS 段与 SOUL 段各自按时间倒序，最新在上）。**本文件自身只在尾部带版本行**（`_版本：x.y`）——段内照抄的历史 `_版本：x.y` 行会与头部版本行混读（`mind-validate` 的头/尾一致性检查取尾部窗口里**第一个**匹配 ⇒ 必误报），故头部**不设**版本行；提版本号照旧（`verify-l1-versions` 取**最后一个** `_版本：`）。本文件此前无版本行 ⇒ 内容一改就被该门禁判红，2026-09-24 注入面体检批次补上。
> 回滚：改前快照在 `mind-private\tasks\evolution\snapshots\`（批次 `l0-slim` 2026-09-23T11-23-27；批次 `l0-polish` 2026-09-24T03-58-18）；`node scripts/evolve-log.mjs rollback --list mind/L0/AGENTS.md`。

---

## AGENTS.md 沿革

（2026-09-23 自 `mind\L0\AGENTS.md` 尾部整行移出，原文照抄，含 3.12 及更早；2026-09-24 追加 3.13；2026-09-25 补记 3.14 / 3.15——此前遗漏，二者只活在 L0 正文尾行，下一版替换即永久丢失）

_版本：3.15 | 2026-09-24 | **规则层订正批次 ③ ⑦**：§三「等放行」补**授权转授**半句（成员在 Lead 冻结范围内可写、不再逐次等主人放行；出范围/高危区照旧走放行——与成员尾注「收到即执行」对齐）· 同批去掉版本行里的私有批次名_

_版本：3.14 | 2026-09-24 | 注入面体检：§四 三档表改行式（判据下沉 `Ritual §四`——同时把原「细则见 Ritual §四」的**悬空指针修成真指针**）· §六 订正 dup-check 为 POST body 形态并补 `topic` 陷阱 · §八 与头部指针去重 + 补 2 行 · 尾行只留本版一行；授权＝主人「随意调整」+ 改前快照_

_版本：3.13 | 2026-09-23 | **§版本沿革移出正文（L0 注入瘦身）**：本文件每会话全文注入，而版本沿革占正文 22.1% 字符 ⇒ 尾部只留本版一行，历史沿革全文移入 `mind/L1/changelog-L0.md`（零内容丢失）。动机＝本文件 §维护 自订的"每行都要值得每个会话看到"。授权＝主人 2026-09-23「你决定」+ 改前快照（批次 l0-slim）_

_版本：3.12 | 2026-09-21 | **§四 自我修改按可逆性分档**（决定3）：原「自我修改硬流程」是一条统一流程（读起来＝红线级硬拦），而机制只到"护栏提示 + 纪律"（`Ritual §四` 自陈非防伪）⇒ 拆成三档表（不可逆→硬拒 / 可逆·高危→提示+快照+validate 且**落盘仍等主人放行** / 可逆·非高危→快照+validate 即闭环），并把「护栏非防伪机制」的诚实标注自 `Ritual` 上提到 L0 可见。**只收窄文本对齐机制，不改任何机制**；§三「等放行」原样保留（行为层铁律）。授权＝主人 2026-09-21 拍板（A）+ 改前快照（私有区） | 3.11 | 2026-09-18 | **§四 层级铁律订正「TOOL 级别」**（规则层订正批次旧批 ④）：原写「L0 宪法（SOUL/AGENTS/**TOOL**）仅主人明确要求时改」与 `TOOL.md` 首行「本文件为 L0 **从属参考**，非宪法级（不注入、改它不按行为宪法审批）」矛盾，且 `mind-guard.js` 的 `inHighRiskyZone` 名单**不含 `TOOL.md`** ⇒ 按机制改为「L0 宪法＝**SOUL + AGENTS**；`TOOL.md` 层属 L0 但是从属参考、非宪法级」（同批 `HUB.md` §四① 同口径）。本次改动授权：护栏面板 ✓（改前状态＝HEAD `1878e73` 的 git 版本，未另留 snapshot——如实标注） | 3.10 | 2026-09-18 | **§六 记忆闭环订正「明示」口径** | 3.9 | 2026-09-18 | **§四 硬流程补高危名单真源**：原写「高危区（L0 宪法 / mind 规则）→ 主人放行」是**宽口径**——门禁实际只拦 `L0` SOUL/AGENTS + `L1` 八件（`TOOL`／`README`/`Tree`/`Dream`/`Learn`／技能／自身记忆本来就不拦）⇒ 改为「高危名单以 `mind-guard.js` 的 `inHighRiskyZone` 为准」并列出名单与非高危面（消除"mind 规则一律等放行"的空头义务；同批 `Ritual:49` 刹闸 / `Power:77` 刹车 同口径）。本次改动授权：护栏面板 ✓ + 改前快照 2026-09-18T02-30-46_AGENTS.md | 3.8 及更早沿革见 changelog_

---

## SOUL.md 沿革

（2026-09-23 自 `mind\L0\SOUL.md` 尾部整行移出，原文照抄，含 2.4 及更早；2026-09-24 追加 2.5；2026-09-25 补记 2.6——此前遗漏，它只活在 L0 正文尾行，下一版替换即永久丢失）

_版本：2.6 | 2026-09-24 | 注入面体检：删演绎条款的头部/§三 重复、例行收敛、尾行只留本版一行（旧沿革入 `mind/L1/changelog-L0.md`）；授权＝主人「随意调整」+ 改前快照（批次 l0-polish）_

_版本：2.5 | 2026-09-23 | **§版本沿革移出正文（L0 注入瘦身）**：本文件每会话全文注入，而版本沿革占正文 26.7% 字符 ⇒ 尾部只留本版一行，历史沿革全文移入 `mind/L1/changelog-L0.md`（零内容丢失）。动机＝本文件首行自订的"每行都要值得每个会话看到"。授权＝主人 2026-09-23「你决定」+ 改前快照（批次 l0-slim）_

_版本：2.4 | 2026-09-21 | **§三 拆分红线**（决定3「按可逆性分档」）：原「红线 — 隐私/双区边界/**自我修改硬流程**：任何情况不绕」把"可逆的自我修改"与"不可逆动作"混成一档，而机制实际只到"护栏提示 + 纪律"（`Ritual §四` 自陈非防伪）⇒ 拆为「① 不可逆红线（机制层硬拒）」+「② 可逆的自我修改（纪律级：快照 + validate）」；同批改 §三 例子的编号引用。**只收窄文本对齐机制，不改任何机制**，也不动"等放行"（行为层铁律，见 `AGENTS §三`）。授权＝主人 2026-09-21 拍板（A）+ 改前快照（私有区） | 2.3 | 2026-09-07 | §三 增「演绎是身份维度、非风格选项」——剔"重活时演绎可被风格末位压掉"的漏洞；措辞抽象、不含任何角色词（双区：角色归人设卡） | 2.2 人格演绎改为「**必须全程演绎**」（原「需要演绎时读它」暗示可选，致重活时人格被"工作模式"压掉）；以人设卡为唯一权威源；演绎不得影响专业正确 | 2.1 增 §三 跨文件总优先级链（红线>原则>协议>动机）；动机去重（实践导向并入原则 2 本地验证）；身份精简（细节归 AGENTS）；§四 动机收敛 3 条_

---

_版本：1.2 | 2026-09-25 | **补记 3 条 L0 沿革断档**（AGENTS 3.14 / 3.15 + SOUL 2.6）——这三条此前只活在 L0 正文尾行，而本文件约定是「正文只留本版一行、被替换下来的旧沿革整行追加到本文件」⇒ 下一版一改即永久丢失；2026-09-25 每日自维护巡检抓出（`Ritual §四` 能处理项：记录类免放行 + 改前快照 `05328192`）_
