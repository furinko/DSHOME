# Learn.md — 痕迹库（出厂模板）

> 版本：1.1 | 2026-09-24 | **补实「记录规则」里的条目纪律**：`Ritual §一 step9` 明写"写自省前先读 `Learn.md` 头部「条目纪律」"，而本文件与私有实例**都没有这一节**（全仓 `grep` 只在 `Ritual.md` 自己命中）⇒ 该消费点指向空处。本次把纪律写进被指向的位置（补实，不改指针）
> 加载：R1 数据源——私有区实际条目由 mind-prime 取最近教训随召回注入；本文件仅格式模板
> 定位：L1 痕迹库——精炼教训（批评/表扬）。**实际条目在 `mind-private\L1\Learn.md`（同名私有优先，永不推送）；本文件为模板。**

## 记录规则

- 被用户批评 / 表扬 → **立即**写入 `mind-private\L1\Learn.md`，不靠"下次记住"。
- 每条四要素（≤200 字）：**时间 / 事件 / 根因 / 正确默认**。
- 批评 →「💢」段落；表扬 →「🤗」段落。
- **同主题复发只追加计数、不新开条目**：写前先 `grep` 已有条目（连日期一起看），命中就把计数并进那一条（形态如「同族第 N 次」）。
- **新条目一律 append 到文件末尾**：上工召回只取**末尾若干条 + 按任务检索**，插进分区中间＝写了却召不回。
- 同类型反复出现 → 提升为 L3 记忆或规则（AGENTS/L2 Skill）。

## 模板

```markdown
## 💢（批评 → 纠正为默认行为）
- [YYYY-MM-DD] <被批评的行为> → <正确的默认做法>

## 🤗（表扬 → 固化为默认行为）
- [YYYY-MM-DD] <被表扬的行为> → <固化的默认做法>
```

---

_实际条目：`mind-private\L1\Learn.md`（含历史 2026-09-01 以来的批评/表扬记录，迁移自 soul\Learn.md）_

---

_版本：1.1 | 2026-09-24 | 与文件头版本行同步（**补实条目纪律**：同主题复发只追加计数 · 新条目 append 到文件末尾——`Ritual §一 step9` 的消费点此前指向空处）_

---

_沿革：1.0 | 2026-09-02 | 初版（2026-09-17 补：本文件此前**缺文件尾版本行**，`verify-l1-versions` 报 warn 后补齐）_
