# L3 — 记忆层（出厂说明）

> 定位：L3 记忆 = index（结晶知识）+ history（时间胶囊）。
> **出厂区（本目录）只放模板与公开示例**——运行时记忆一律在 `$DSH_HOME\mind-private\L3\`（仓库同级 `mind-private\L3\`，隐私，永不推送，同名私有优先）。
> 归档规则见 `mind\L1\Memory.md`；主题索引 = `mind-private\L3\index\<主题>\` 各目录 `_index.md`（Memory §四 格式）；主题总览在 `mind\L1\Tree.md` L3 区（无顶层独立总索引，避免三重索引冗余）。
