# deps
node_modules/

# logs / tmp
*.log
*.tmp
node_modules/.cache/electron/

# dshome local dump/describe artifacts
describe.json
dump-*.log
*.backend.log

# OS
.DS_Store
Thumbs.db

# local build/install artifacts
*.tsbuildinfo
*.tgz
!vendor/*.tgz

# dsh runtime data (generated on first boot)
settings.yaml
sessions/
storages/
.anonymous-user-id
profiles/*/cordis.yml
attachments/
llm-deepseek/

# dev launcher log
dsh-server.log

# dsh credentials store (API keys, zero-credential rule)
.credentials.yaml

# dsh-evolve private workspace (own git repo + memory mirror + backups)
evolve-workspace/

# fish mind: private runtime half (memory/projects/learn — never push)
mind-private/

# soul: behavior learning is private (only template was ever pushed)
soul/

# dsh-evolve usage stats sidecar (changes on every load)
skills/.evolve-usage.json

# dev launcher backups
开发启动.cmd.bak-*

# dshome runtime state (regenerated on boot)
.agent-presets/
.dshw-*.json
profiles/*/.dsh-market/
router-standard/

# 发布构建产物（Inno payload + setup exe）；DSHOME.iss 例外入库（打包配置纳入版本管理）
build-stage/*
!build-stage/DSHOME.iss
