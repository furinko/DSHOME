# deps
node_modules/

# logs / tmp
*.log
*.tmp
node_modules/.cache/electron/

# dshome local dump/describe artifacts
describe.json
dump-*.log
*.backend.log

# OS
.DS_Store
Thumbs.db

# local build/install artifacts
*.tsbuildinfo
*.tgz
!vendor/*.tgz

# dsh runtime data (generated on first boot)
settings.yaml
sessions/
storages/
.anonymous-user-id
profiles/*/cordis.yml
attachments/
llm-deepseek/

# dev launcher log
dsh-server.log

# dsh credentials store (API keys, zero-credential rule)
.credentials.yaml

# dsh-evolve private workspace (own git repo + memory mirror + backups)
evolve-workspace/

# fish mind: private runtime half (memory/projects/learn — never push)
mind-private/

# 私有暂存区（历史资产暂存；含私有项目名/本机路径 — never push）
/私有暂存区/

# self-modification snapshots (AGENTS hard-flow working dir — never push)
.agent-snapshot/

# soul: behavior learning is private (only template was ever pushed)
soul/

# dsh-evolve usage stats sidecar (changes on every load)
skills/.evolve-usage.json

# 备份文件（<原名>.bak / <原名>.bak-<时间戳>）—— 勿用 git add -A 误带
#   沿革：09-09 只枚举了「开发启动.cmd.bak-*」；09-12 同类第 2 次（shell-app/main.cjs.bak-*）漏网
#   ⇒ 枚举必漏，改按后缀通用忽略（加前已核实：全仓 0 个被跟踪 .bak，规则零副作用）
开发启动.cmd.bak-*
*.bak
*.bak-*

# dshome runtime state (regenerated on boot)
.agent-presets/
.dshw-*.json
profiles/*/.dsh-market/
router-standard/

# 发布构建产物（Inno payload + setup exe）；DSHOME.iss 例外入库（打包配置纳入版本管理）
build-stage/*
!build-stage/DSHOME.iss
