# 解锁 UI 填 API Key：apiKeyEnv 指向不存在的变量名（DSHOME_USER_KEY），
# 官方 UI 判定「引用的 env 无值 → 需要用户提供」→ 输入框可编辑，
# 用户填的 key 存 dsh 凭证库（dsh-credentials-local），之后自动解析。
# 若 apiKeyEnv 指向真实存在的环境变量，UI 会锁定只读「由启动环境提供」。
- id: llm-deepseek
  config:
    apiKeyEnv: DSHOME_USER_KEY
